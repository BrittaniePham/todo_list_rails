<h1>New Todo List</h1>
<%= form_for @todo do |f| %> <%# rails helper that creates a form %>
  <%= f.label :title %> 
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.label :completed %>
  <%= f.check_box :completed %>

  <%= f.submit %>
<% end %>